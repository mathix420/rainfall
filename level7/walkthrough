1. La vulnerabilite est encore strcpy, cette fois on doit l'utiliser pour executer la fonction `m` afin d'afficher la chaine `c` precedament lu dans le fichier `.pass` du level9.
2. En affichant la heap grace a gdb (`x/32wx 0x804a000`) on comprends qu'en overridant le 6e octet on peut changer l'addresse d'ecriture du second `strcpy`.
3. On choisit donc d'override l'addresse de puts dans la `GOT` afin de lancer la fonction `m` a la place.

> Flag: 5684af5cb4c8679958be4abe6373147ab52d95768e047820bf382e44fa8d8fb9
